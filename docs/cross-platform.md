# Cross-Platform Notes

- Keep all public paths relative.
- Prefer UTF-8 for every text file.
- Use `python -X utf8` on Windows when you need strict UTF-8 behavior.
- Use `python3` on macOS and Linux when that is the active interpreter name.
- Do not encode public behavior around drive letters, usernames, or machine-specific caches.

